// Map function will an array to map and a callback function
const words = ['ground', 'control', 'to', 'major', 'tom'];

const map = function(){
  
}

const results1 = map(words, word => word[0]);
console.log(results1);